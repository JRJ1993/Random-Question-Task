import StaticMath from '../StaticMath/StaticMath'

const SimultaneousEquationsQuestion = () => {
    return (
        <StaticMath latex={`\\text{Generate a random question and display it here.}`} />
    )
}

export default SimultaneousEquationsQuestion

